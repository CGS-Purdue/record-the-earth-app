import { ThemeColors } from '../Colors';
import { Layout } from '../Layout';

// flex: 14,
const MainLogo = {
  width: 200,
  height: 200,
  resizeMode: 'contain',
};

const RecStartBtn = {
  width: 200,
  height: 200,
  maxWidth: '100%',
  maxHeight: '100%',
  resizeMode: 'contain',
};

const MainStyles = {
  logo_main: MainLogo,
  btn_rec_start: RecStartBtn,
};

export { MainStyles };
