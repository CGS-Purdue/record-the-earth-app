import { ThemeColors } from '../Colors';
import { ThemeLayout } from '../Layout';

// flex: 14,
const MainLogo = {
  width: 300,
  height: 200,
  resizeMode: 'contain',
};

const RecStartBtn = {
  width: 200,
  height: 200,
  maxWidth: '66%',
  maxHeight: '66%',
  resizeMode: 'contain',
};

const MainStyles = {
  logo_main: MainLogo,
  btn_rec_start: RecStartBtn,
};

export { MainStyles };
