import { ThemeColors } from '../Colors';
import { Layout } from '../Layout';

const MainLogo = {
  flex: 1,
  width: 300,
  height: '70%',
  resizeMode: 'contain',
};


const MainStyles = {
  logo_main: MainLogo,
};

export { MainStyles };
